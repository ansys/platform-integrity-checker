apiVersion: v1
kind: Service
metadata:
  name: {{ include "pic-backend.fullname" . }}
  labels:
    {{- include "pic-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pic-backend.selectorLabels" . | nindent 4 }}
