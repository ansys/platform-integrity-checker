apiVersion: v1
kind: Service
metadata:
  name: {{ include "pic-frontend.fullname" . }}
  labels:
    {{- include "pic-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pic-frontend.selectorLabels" . | nindent 4 }}
