apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pic-backend.fullname" . }}-test-connection"
  labels:
    {{- include "pic-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pic-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
